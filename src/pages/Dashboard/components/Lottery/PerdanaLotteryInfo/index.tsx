import BaseLotteryInfo from "../BaseLotteryInfo";
import { PerdanaLotteryInfoProps } from "./type";

function PerdanaLotteryInfo(props: PerdanaLotteryInfoProps) {
  return <BaseLotteryInfo {...props} />;
}

export default PerdanaLotteryInfo;
